{
	"name": "root",
	"version": "0.0.0",
	"private": true,
	"author": "Microsoft Corporation",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/microsoft/vscode-webview-ui-toolkit.git"
	},
	"workspaces": [
		"./packages/toolkit",
		"./packages/toolkit-react"
	],
	"scripts": {
		"build": "npm run build --if-present --workspaces",
		"fmt": "prettier --config ./.prettierrc --check \"**/*.{ts,js,md}\"",
		"fmt:fix": "prettier --config ./.prettierrc --write \"**/*.{ts,js,md}\"",
		"lint": "npm run lint --if-present --workspaces",
		"lint:fix": "npm run lint:fix --if-present --workspaces",
		"test": "echo 'Todo: add tests'"
	},
	"devDependencies": {
		"prettier": "^2.2.1"
	}
}
